apiVersion: v1
kind: Service
metadata:
  name: gym-membership
  namespace: gym-mgmt
  labels:
    app: gym-membership
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: gym-membership
